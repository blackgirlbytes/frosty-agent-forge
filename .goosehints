every time you  make a change, make a commit.

for any long running / blocking commands like npm run start or npm run dev or any interactive commands npm install, ask the user to run it instead of you running it. 

dont write summaries of what you did and put it in markdown. 